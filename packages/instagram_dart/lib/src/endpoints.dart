/// The URL to access to receive an Instagram authorization token.
final Uri authorizationEndpoint = Uri.parse('https://api.instagram.com/oauth/authorize');

/// The URL to access to exchange an authorization token for an access token.
final Uri tokenEndpoint = Uri.parse('https://api.instagram.com/oauth/access_token');